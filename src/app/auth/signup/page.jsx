import React from 'react';

import { RegisterForm } from '@features/auth';

const SignUp = () => {
    return (
        <div>
            <RegisterForm />
        </div>
    );
};

export default SignUp;
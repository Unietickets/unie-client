import React from 'react';

import { Container } from '@shared/ui';

const OwnTicketLayout = ({ children }) => {
  return (
    <Container>
      {children}
    </Container>
  );
};

export default OwnTicketLayout;

import React from 'react';

import { Container } from '@shared/ui';

const CreateTicketLayout = ({ children }) => {
  return (
    <Container>
      {children}
    </Container>
  );
};

export default CreateTicketLayout;
